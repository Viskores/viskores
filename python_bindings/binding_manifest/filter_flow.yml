##=============================================================================
##
##  The contents of this file are covered by the Viskores license. See
##  LICENSE.txt for details.
##
##  By contributing to this file, all contributors agree to the Developer
##  Certificate of Origin Version 1.1 (DCO 1.1) as stated in DCO.txt.
##
##=============================================================================

- class: viskores::filter::flow::FilterParticleAdvection
  binding: excluded
  reason: Flow filters are not part of the initial Python binding scope.

- class: viskores::filter::flow::Lagrangian
  binding: excluded
  reason: Flow filters are not part of the initial Python binding scope.

- class: viskores::filter::flow::LagrangianStructures
  binding: excluded
  reason: Flow filters are not part of the initial Python binding scope.

- class: viskores::filter::flow::ParticleAdvection
  binding: excluded
  reason: Flow filters are not part of the initial Python binding scope.

- class: viskores::filter::flow::PathParticle
  binding: excluded
  reason: Flow filters are not part of the initial Python binding scope.

- class: viskores::filter::flow::Pathline
  binding: excluded
  reason: Flow filters are not part of the initial Python binding scope.

- class: viskores::filter::flow::StreamSurface
  binding: excluded
  reason: Flow filters are not part of the initial Python binding scope.

- class: viskores::filter::flow::Streamline
  binding: excluded
  reason: Flow filters are not part of the initial Python binding scope.

- class: viskores::filter::flow::WarpXStreamline
  binding: excluded
  reason: Flow filters are not part of the initial Python binding scope.
