## Initial Python Bindings

Viskores now provides an initial Python bindings package under `python_bindings/`.
You can build the extension in-tree for development, run the included smoke test
and demo scripts, and produce a wheel that links against an installed Viskores
build.
